name: kantan
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.4
  http: ^1.6.0
  kana_kit: ^2.1.1
  path_drawing: ^1.0.1
  xml: ^6.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.30.0

flutter:
  uses-material-design: true

  assets:
    - assets/
